import {Button, HStack} from "@chakra-ui/react";

const App = () => {
    return(
        <HStack>
            <Button colorPalette={"blue"}>Click Me</Button>
        </HStack>
    )
}

export default App;